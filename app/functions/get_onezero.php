<?php
// Returns 1 or 0
function get_onezero($number)
{
    error('GET_ONEZERO is no longer used. use SBC::oneZero() instead');

/*
    $number = isset($number) ? (int) $number : 0;
    if ($number != 1)
    {
        $number = 0;
    }
    return $number;

*/
}